# Agent Notes

- Treat this directory as the new Sightlines rebuild repository.
- Use `sightlines-old/` only as local reference material for product behavior, copy, architecture, and implementation details.
- Do not commit files from `sightlines-old/` into this repository unless the user explicitly asks to migrate specific code or assets.

