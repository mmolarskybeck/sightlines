# Sightlines

Rebuild workspace for Sightlines.

The previous application is kept locally at `./sightlines-old/` as a separate Git repository for reference. It is intentionally ignored by this new repository so the rebuild can have clean history while the old commits and files remain available on this machine.

